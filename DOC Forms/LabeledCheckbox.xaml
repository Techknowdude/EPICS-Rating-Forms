<UserControl x:Class="DOC_Forms.LabeledCheckbox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DOC_Forms"
             mc:Ignorable="d" 
             d:DesignHeight="26" MinWidth="80" 
             HorizontalAlignment="Stretch"
             FontSize="10"
             Margin="5,0">
    <StackPanel Orientation="Horizontal" x:Name="MainPanel">
        <CheckBox x:Name="CheckBox" VerticalAlignment="Center" IsChecked="{Binding Checked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Checked="CheckBox_OnChecked"></CheckBox>
        <Label x:Name="Label" VerticalAlignment="Center" Content="{Binding LabelContent}"></Label>
        
    </StackPanel>
</UserControl>
